import './EmptyState.scss';

function EmptyState() {
    return (
        <div className="empty-state">
            <p>Nothing new in the last 48 hours — check back soon.</p>
        </div>
    );
}

export default EmptyState;
